version: "1.0"

services:
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - 80:80
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - 8080:8080